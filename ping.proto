syntax = "proto3";
option go_package = "./buf";

package ping;

service PingService {
    rpc SendPing (PingRequest) returns (PingResponse) {}
}

message PingRequest {
    string message = 1;
}

message PingResponse {
    string reply = 1;
}
